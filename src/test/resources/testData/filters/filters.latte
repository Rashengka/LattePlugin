{* Test file for Latte filters *}

{* String case manipulation filters *}
<p>Uppercase: {$text|upper}</p>
<p>Lowercase: {$text|lower}</p>
<p>First letter uppercase: {$text|firstUpper}</p>
<p>Capitalize each word: {$text|capitalize}</p>

{* HTML and URL escaping filters *}
<p>HTML escaped: {$html|escape}</p>
<p>URL escaped: {$url|escapeUrl}</p>
<p>No escaping: {$html|noescape}</p>

{* String manipulation filters *}
<p>Truncated: {$longText|truncate:30}</p>
<p>Truncated with custom ending: {$longText|truncate:30:'...'}</p>
<p>Substring: {$text|substring:0:5}</p>
<p>Trimmed: {$text|trim}</p>
<p>Padded left: {$number|padLeft:5:'0'}</p>
<p>Padded right: {$number|padRight:5:'0'}</p>
<p>Replace: {$text|replace:'old':'new'}</p>
<p>Strip HTML: {$html|stripHtml}</p>
<p>Strip tags: {$html|strip}</p>
<p>Indented: {$text|indent:4}</p>
<p>Reversed: {$text|reverse}</p>
<p>Length: {$text|length}</p>
<p>Webalize: {$text|webalize}</p>

{* Formatting filters *}
<p>Date format: {$date|date:'j.n.Y H:i:s'}</p>
<p>Number format: {$number|number:2:',':' '}</p>
<p>Bytes format: {$bytes|bytes}</p>
<p>Percent format: {$ratio|percent}</p>

{* Array and collection filters *}
<p>Join array: {$array|join:', '}</p>
<p>Implode array: {$array|implode:', '}</p>
<p>Explode string: {$text|explode:','}</p>
<p>Sort array: {$array|sort}</p>
<p>Reverse array: {$array|reverse}</p>

{* Conditional filters *}
<p>Default value: {$nullValue|default:'Not set'}</p>
<p>Check empty: {$value|checkEmpty}</p>

{* Multiple filters *}
<p>Multiple filters: {$text|upper|truncate:20}</p>
<p>Multiple filters with parameters: {$text|replace:'old':'new'|truncate:30:'...'}</p>

{* Custom filters *}
<p>Custom filter: {$text|myCustomFilter}</p>
<p>Custom filter with parameters: {$text|myCustomFilter:10:'prefix'}</p>

{* Filters with expressions *}
<p>Filter with expression: {($a + $b)|number:2}</p>
<p>Filter with complex expression: {($user->isAdmin() ? 'Admin' : 'User')|upper}</p>

{* Filters in attributes *}
<a href="{$url|escapeUrl}" title="{$title|escape}">Link</a>

{* Filters with n:attributes *}
<div n:attr="data-value => $value|escape, title => $title|truncate:20"></div>

{* Filters in loops *}
<ul>
{foreach $items as $item}
    <li>{$item|capitalize}</li>
{/foreach}
</ul>